"use strict";//treats all JS code as  newew version 

//alert( 3 + 3) // we are using node.js not the brwser so it will not throw the anwer


"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high so we write 
     console.log(3 + 3)

console.log("minhaj")


let name = "minhaj"
let age = 18
let isLoggedIn = false

// number => 2 to power 53 the values it can take upto
// bigint used where the numbers are too big  such as on fb
// string => "" can decalare in single quoteas well
// boolean => true/false used almost in every langauge
// null => standalone value means empty usch as if the temperature using weather forcast is not fetched
// undefined =>  
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
